# name: KA Galintine - Style Transform

# on:
#   schedule:
#     - cron: '*/5 7-16 * * 1-5'  # every 5 min, 7am-4pm UTC (8am-5pm WAT), Mon-Fri
#   workflow_dispatch:

# jobs:
#   transform:
#     runs-on: ubuntu-latest

#     concurrency:
#       group: style-transform
#       cancel-in-progress: false

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'

#       - name: Cache pip dependencies
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/pip
#           key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
#           restore-keys: |
#             ${{ runner.os }}-pip-

#       - name: Install dependencies
#         run: pip install google-genai google-auth google-auth-oauthlib google-api-python-client pillow tqdm==4.67.3 python-dotenv==1.2.1

#       - name: Run style transform
#         env:
#           GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
#           GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
#           GOOGLE_TOKEN_JSON: ${{ secrets.GOOGLE_TOKEN_JSON }}
#         run: python src/main.py

#       - name: Delete this workflow run (keep history clean)
#         if: always()
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: gh run delete ${{ github.run_id }} --repo ${{ github.repository }} || echo "Skipping delete"